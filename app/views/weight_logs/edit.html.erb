<h1>体重履歴の変更</h1>

<%= form_for @weight_log do |f| %>
  <% if @weight_log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@weight_log.errors.count, "error") %> prohibited this weight log from being saved:</h2>

      <ul>
      <% @weight_log.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
    計測日：<%= f.date_select :measured_date %>
  </div>
  <div class="field">
    体重：<%= f.number_field :weight , :step => 0.1 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
